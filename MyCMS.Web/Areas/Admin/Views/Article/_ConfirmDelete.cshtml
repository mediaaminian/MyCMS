<div class="container-fluid">
    <div class="row-fluid">
        <div class="span12">
            <div class="alert">
                <strong>هشدار!</strong> آیا شما از حذف این صفحه مطمئن هستید؟
            </div>
            @using (Ajax.BeginForm(MVC.Admin.Article.ActionNames.Delete, MVC.Admin.Article.Name, new { id = ViewBag.Id }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, OnBegin = "showLoading", UpdateTargetId = "result", OnSuccess = "loadAjaxComponents", OnComplete = "hideLoading" }, null))
            {
                @Html.AntiForgeryToken()
                <div id="result"></div>
                <input type="submit" class="btn btn-success" value="بله" />
                <input type="button" id="btnClose" class="btn btn-danger" name="btnCancel" value="انصراف" onclick="$dialog.dialog('close');" />
            }
        </div>
    </div>
</div>
